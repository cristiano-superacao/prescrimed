# Produção
# Se o frontend estiver hospedado SEPARADO (Netlify / Railway static / GitHub Pages),
# aponte para o backend público no Railway:
VITE_BACKEND_ROOT=https://prescrimed-backend-production.up.railway.app

# Se o frontend estiver sendo servido pelo MESMO serviço Node do backend,
# você pode usar o proxy relativo abaixo (descomentando):
# VITE_API_URL=/api

# Para desenvolvimento local em porta customizada:
# VITE_API_URL=http://localhost:8000/api
