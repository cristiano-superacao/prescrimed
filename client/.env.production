# Production Environment Variables for Railway
# Este arquivo define a URL do backend para builds de produção

# URL completa da API (backend no Railway serve na mesma origem)
VITE_API_URL=/api

# URL raiz do backend (para health checks e outros endpoints)
VITE_BACKEND_ROOT=https://prescrimed.up.railway.app
