# Railway - Backend e Frontend no mesmo serviço
# Usa URL relativa para API (mesmo domínio)
VITE_API_URL=/api

# URL do backend para healthcheck (quando backend está em serviço separado)
# Ajuste para o domínio público do backend (serviço "prescrito")
# Exemplo: https://prescrimed-production.up.railway.app
VITE_BACKEND_ROOT=https://prescrimed-production.up.railway.app
