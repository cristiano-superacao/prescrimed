# Variáveis de Ambiente - Railway Frontend

# Base path para Railway (raiz do domínio)
VITE_BASE=/

# Backend está no mesmo serviço (servidor Node.js serve frontend + backend)
# Usa caminho relativo para API
VITE_API_URL=/api

# Backend Railway (URL pública do serviço nodejs)
VITE_BACKEND_ROOT=https://nodejs-production-31f3.up.railway.app

# Node Environment
NODE_ENV=production
