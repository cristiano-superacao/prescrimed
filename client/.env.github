# GitHub Pages Environment
# Frontend hospedado no GitHub Pages precisa apontar para backend no Railway

# URL completa da API no Railway (backend separado)
VITE_API_URL=https://prescrimed.up.railway.app/api

# URL raiz do backend no Railway
VITE_BACKEND_ROOT=https://prescrimed.up.railway.app
