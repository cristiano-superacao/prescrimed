[phases.setup]
# Garante Python disponível no ambiente Nixpacks
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools", "python311Packages.wheel"]

[phases.install]
cmds = [
  "python -m pip install -r requirements.txt"
]

[start]
cmd = "python -m streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
