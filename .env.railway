# Railway Environment Variables Template
# Copie este arquivo e configure no Railway Dashboard

# Port (Railway configura automaticamente)
PORT=3000

# MongoDB Configuration
# OPÇÃO 1: Usar MongoDB Memory Server (temporário - dados não persistem)
USE_MEMORY_DB=true

# OPÇÃO 2: MongoDB Atlas (recomendado para produção)
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/prescrimed

# OPÇÃO 3: MongoDB Railway (se adicionar database MongoDB)
# Será criado automaticamente como MONGO_URL

# JWT Secret (obrigatório - gerar string aleatória segura)
JWT_SECRET=seu-secret-muito-seguro-aqui-mude-isso

# Node Environment
NODE_ENV=production

# CORS Origins (opcional - por padrão permite todos)
# CORS_ORIGIN=https://prescrimed.netlify.app

# Log Level (opcional)
# LOG_LEVEL=info
