# Variáveis de Ambiente — API (.env)
# Copie para .env e preencha.

# Porta e ambiente
NODE_ENV=development
PORT=3000

# Sincronização de modelos em produção (use com cautela)
# true para criar/alterar tabelas na primeira implantação
FORCE_SYNC=false

# JWT (somente para desenvolvimento local)
JWT_SECRET=dev-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
SESSION_TIMEOUT=8h

# Frontend local
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,https://cristiano-superacao.github.io,https://cristiano-superacao.github.io/prescrimed

# PostgreSQL - Local
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=prescrimed

# PostgreSQL - Railway (DATABASE_URL é fornecida automaticamente)
# DATABASE_URL=postgresql://user:pass@host:port/database

# Domínio público do Railway (opcional)
# Preenchido automaticamente em produção; útil para CORS
# RAILWAY_PUBLIC_DOMAIN=prescrimed-backend-production.up.railway.app

# CORS — origens adicionais separadas por vírgula (opcional)