# Variáveis de Ambiente — API (.env)
# Copie para .env e preencha.

# Porta e ambiente
PORT=3000
NODE_ENV=production

# MongoDB (Atlas ou plugin Railway) — use uma destas variáveis
# Ex.: mongodb+srv://usuario:senha@cluster.mongodb.net/prescrimed?retryWrites=true&w=majority
MONGODB_URI=
MONGO_URL=
MONGODB_URL=
DATABASE_URL=

# JWT Secret (use um base64 de 32+ caracteres)
JWT_SECRET=

# CORS — origem do frontend (sem barra final)
# Ex.: https://cristiano-superacao.github.io ou https://seuapp.netlify.app
FRONTEND_URL=

# Railway — domínio público (preenchido automaticamente)
RAILWAY_PUBLIC_DOMAIN=

# CORS — origens adicionais separadas por vírgula (opcional)
ALLOWED_ORIGINS=