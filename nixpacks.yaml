# Arquivo desativado: use apenas nixpacks.toml
# Este arquivo existia com conteúdo em TOML mas nome .yaml incorreto,
# o que pode causar detecção incorreta. Mantido vazio/neutralizado.
NODE_VERSION = "18"
