# Variáveis de Ambiente — Produção (Railway)
# Copie para o dashboard do Railway (serviço Backend)

# Porta (definida automaticamente pelo Railway)
# PORT=${PORT}

# Banco de Dados (PostgreSQL)
# Fornecido pelo plugin PostgreSQL do Railway
DATABASE_URL=postgresql://usuario:senha@host:5432/prescrimed

# Sincronização inicial (use apenas no primeiro deploy e remova depois)
# FORCE_SYNC=true

# JWT
JWT_SECRET=<gere_com_openssl_rand_base64_32>
JWT_REFRESH_SECRET=<gere_com_openssl_rand_base64_32>
SESSION_TIMEOUT=8h

# CORS / Frontend
# Caso possua frontend separado
# FRONTEND_URL=https://prescrimed.netlify.app
# ALLOWED_ORIGINS=https://prescrimed.netlify.app,https://seu-dominio.com

# Ambiente de execução
NODE_ENV=production

# Observações:
# - RAILWAY_PUBLIC_DOMAIN é injetada automaticamente pelo Railway e já é considerada no CORS do backend
# - O frontend construído (Vite) será servido pelo Express a partir de client/dist
